diff --git a/src/db/repositories/posts.repository.ts b/src/db/repositories/posts.repository.ts
index 0870d3b..0e27d3d 100644
--- a/src/db/repositories/posts.repository.ts
+++ b/src/db/repositories/posts.repository.ts
@@ -27,7 +27,13 @@ export function createPostsRepository(db: DbClient) {
       return db.insert(posts).values(input);
     },
     async update(id: number, patch: Partial<typeof posts.$inferInsert>) {
-      return db.update(posts).set(patch).where(and(eq(posts.id, id)));
+      return db
+        .update(posts)
+        .set({
+          ...patch,
+          updatedAt: new Date().toISOString()
+        })
+        .where(and(eq(posts.id, id)));
     },
     async remove(id: number) {
       return db.delete(posts).where(eq(posts.id, id));
diff --git a/src/pages/api/admin/posts/[id]/update.ts b/src/pages/api/admin/posts/[id]/update.ts
index 1ec1c32..17e66b8 100644
--- a/src/pages/api/admin/posts/[id]/update.ts
+++ b/src/pages/api/admin/posts/[id]/update.ts
@@ -45,6 +45,11 @@ export const POST: APIRoute = async ({ params, request, locals, redirect }) => {
     return new Response('Post not found', { status: 404 });
   }
 
+  const slugTaken = await repo.getBySlug(slug);
+  if (slugTaken && slugTaken.id !== postId) {
+    return new Response('Slug already exists, please use another slug', { status: 409 });
+  }
+
   const publishedAt =
     status === 'published'
       ? existing.publishedAt ?? new Date().toISOString()
diff --git a/src/pages/api/admin/posts/create.ts b/src/pages/api/admin/posts/create.ts
index 331fc85..a468303 100644
--- a/src/pages/api/admin/posts/create.ts
+++ b/src/pages/api/admin/posts/create.ts
@@ -35,6 +35,11 @@ export const POST: APIRoute = async ({ request, locals, redirect }) => {
   }
 
   const repo = createPostsRepository(db);
+  const slugTaken = await repo.getBySlug(slug);
+  if (slugTaken) {
+    return new Response('Slug already exists, please use another slug', { status: 409 });
+  }
+
   await repo.create({
     title,
     slug,
